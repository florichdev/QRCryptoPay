# 🚀 Инструкция по настройке CryptoPay

## Быстрый старт

### 1. Создайте конфигурационный файл

```bash
# Скопируйте пример конфигурации
copy cfg.example.py cfg.py
```

### 2. Заполните cfg.py своими данными

Откройте `cfg.py` и замените:

```python
# Telegram Bot
TELEGRAM_BOT_TOKEN = 'ваш_токен_от_@BotFather'
TELEGRAM_BOT_USERNAME = 'имя_вашего_бота'

# Solana
ADMIN_WALLET = "ваш_публичный_адрес_кошелька"
ADMIN_PRIVATE_KEY = "ваш_приватный_ключ"

# Администраторы и воркеры (Telegram ID)
ADMIN_IDS = [ваш_telegram_id]
WORKER_IDS = [telegram_id_воркера]
```

### 3. Как получить Telegram ID?

Напишите боту [@userinfobot](https://t.me/userinfobot) и он покажет ваш ID.

### 4. Как создать Telegram бота?

1. Напишите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям
4. Скопируйте полученный токен в `cfg.py`

### 5. Solana кошелек

Для тестирования используйте `devnet`:
```python
SOLANA_NETWORK = "devnet"
```

Для продакшена:
```python
SOLANA_NETWORK = "mainnet"
```

⚠️ **ВАЖНО:** Никогда не публикуйте `cfg.py` в git!

### 6. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 7. Запуск

```bash
python run.py
```

## Дополнительно

### SSL сертификаты (для HTTPS)

Создайте папку `ssl/` и поместите туда:
- `localhost.crt` - сертификат
- `localhost.key` - приватный ключ

Или используйте скрипт для генерации самоподписанного сертификата.

### Структура базы данных

База данных создастся автоматически при первом запуске в файле `cryptopay.db`.

## Поддержка

По вопросам: [@vflorich](https://t.me/vflorich)
